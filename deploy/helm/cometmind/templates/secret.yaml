apiVersion: v1
kind: Secret
metadata:
  name: {{ include "cometmind.fullname" . }}-secrets
  labels:
    {{- include "cometmind.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secrets }}
  {{- if $value }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
  {{- end }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "cometmind.fullname" . }}
  labels:
    {{- include "cometmind.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cometmind.selectorLabels" . | nindent 4 }}
